function factorial() {
  for (let i = 0; i < 6; i += 1) {
  console.log(i * (i + 1));
  }
}

factorial();

/* function factorial(number) {
  let result = 1;
  for (let counter = number; counter > 0; counter -= 1) {
    result *= counter;
  }

  return result;
} 
*/